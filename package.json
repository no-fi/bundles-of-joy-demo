{
  "name": "react-rotate",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "budo index.js --live -- -t babelify",
    "prebuild": "rm -rf build && mkdir build",
    "build": "browserify index.js -t babelify | uglifyjs > build/bundle.js"
  },
  "author": "Chris Kiel",
  "license": "ISC",
  "dependencies": {
    "preact": "^7.1.0",
    "react-dom": "^15.4.2"
  },
  "devDependencies": {
    "babel-preset-es2015": "^6.22.0",
    "babel-preset-react": "^6.22.0",
    "babelify": "^7.3.0",
    "budo": "^9.4.7",
    "standard": "^8.6.0",
    "uglifyjs": "^2.4.10"
  }
}
